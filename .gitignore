# Secretos de docker-compose (ver .env.example para la plantilla).
/.env

# Skills de Claude Code (clonadas desde GitHub vía `npx skills add`).
# Reproducibles desde skills-lock.json — no se versionan.
/.agents/
/.claude/skills/

# Sistema operativo / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
/commit-message.md

# Plantilla de terceros (Creative Tim, Paper Dashboard 2) usada solo como
# referencia visual para el design system del frontend - ver
# frontend/design-system/MASTER.md. No es parte del producto, no se
# versiona (licencia propia del template).
/paper-dashboard-master/
CLAUDE_OLD.md
